import requests from "../utilities/requests";

function Nav() {
  return (
    <nav>
      <div>
        {/* {Object.entries(requests).map(([, { title, url }]) => {
          <h2>{title}</h2>;
        })} */}
      </div>
    </nav>
  );
}

export default Nav;
